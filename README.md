# git-sync

Just a docker image with git-sync, as minimal as possible. Just with
minimal alpine, nothing fancy.

## Features

- `ca-certificates` to support https repos
- `git-lfs`
- `curl` and `bash` for git hooks and extras
